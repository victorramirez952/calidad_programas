Mientras (Longitud de fileName sea igual a 0)
    Imprimir "Ingresa el nombre del archivo"
    Leer fileName
FinMientras
Si (fileManager.setFile(fileName) es falso) Entonces:
    continuar con la siguiente iteracion
FinSi
content <- fileManager.getFile().getContent()
data = reader.analyzeContent(content)
fileManager.getFile().updateData(data)
fileManager.getFile().showData()
